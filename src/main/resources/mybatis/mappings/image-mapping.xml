<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="image">
	<insert id = "insertImg" parameterType="image">
		INSERT INTO REPLY(PLACE_NO, IMAGE_PATH) VALUE(#{placeNo}, #{imagePath})
	</insert>
	<select id = "selectImgsByPlaceNo" parameterType="int" resultType="image">
		SELECT * FROM IMAGE WHERE PLACE_NO = #{placeNo}  
	</select>
</mapper>